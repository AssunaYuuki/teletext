<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>🛠️ Админ-панель</title>
  <link rel="stylesheet" href="/global.css">
  <style>
    .admin-container { max-width: 1000px; margin: 0 auto; padding: 20px; }
    .archive-card {
      background: var(--card-bg);
      border: 1px solid var(--accent);
      border-radius: 4px;
      padding: 16px;
      margin-bottom: 16px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .archive-info {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--fg);
    }
    .archive-path {
      font-family: var(--font-mono);
      font-size: 0.9rem;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .logo-status {
      color: #ff6666;
      font-size: 0.85rem;
      margin-left: 8px;
    }
    .edit-btn {
      background: var(--hover-bg);
      border: 1px solid var(--accent);
      color: var(--accent);
      padding: 8px 16px;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.2s ease;
    }
    .edit-btn:hover {
      background: var(--accent);
      color: var(--bg);
    }
    h1 {
      text-align: center;
      color: var(--accent);
      margin-bottom: 10px;
    }
    .subtitle {
      text-align: center;
      opacity: 0.7;
      margin-bottom: 20px;
    }
    .folder-icon {
      color: #ffcc00;
      margin-right: 4px;
    }
  </style>
</head>
<body class="crt">
  <header>
    <h1>🛠️ Админ-панель</h1>
    <p class="subtitle">Выберите архив для редактирования</p>
  </header>

  <div class="admin-container">
    <% if (archives.length === 0) { %>
      <div class="empty">
        <p>📁 Нет архивов. Положите папки в <code>teletext/страна/архив/</code></p>
      </div>
    <% } else { %>
      <% archives.forEach(arch => { %>
        <div class="archive-card">
          <div class="archive-info">
            <span class="folder-icon">📁</span>
            <span class="archive-path"><%= arch.path.replace(/\//g, ' → ') %></span>
            <% if (!arch.hasLogo) { %>
              <span class="logo-status">❌ Логотип отсутствует</span>
            <% } else { %>
              <span class="logo-status">✅ Логотип установлен</span>
            <% } %>
          </div>
          <a href="/admin/edit/<%= encodeURIComponent(arch.path) %>" class="edit-btn">✏️ Редактировать</a>
        </div>
      <% }) %>
    <% } %>

    <div style="margin-top: 30px;">
      <a href="/" class="btn">← На главную</a>
    </div>
  </div>
</body>
</html>