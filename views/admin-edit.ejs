<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>✏️ Редактировать — <%= archivePath %></title>
  <link rel="stylesheet" href="/global.css">
  <style>
    .admin-container { max-width: 800px; margin: 0 auto; padding: 20px; }
    .logo-preview { text-align: center; margin: 20px 0; }
    .logo-preview img, .logo-preview svg { max-width: 200px; max-height: 100px; border:1px solid var(--accent); border-radius:4px; }
    .upload-form { background: var(--card-bg); padding: 20px; border-radius: 4px; }
    .path-display { font-family: var(--font-mono); font-size: 0.9rem; color: var(--accent); margin-bottom: 10px; }
  </style>
</head>
<body class="crt">
  <header>
    <h1>✏️ Редактировать</h1>
    <p class="subtitle">Путь: <code><%= archivePath %></code></p>
  </header>

  <div class="admin-container">
    <div class="logo-preview">
      <% if (hasLogo) { %>
        <p>Текущий логотип:</p>
        <img src="<%= logoUrl %>?t=<%= Date.now() %>" alt="Логотип" style="border:1px solid var(--accent); border-radius:4px;">
        <form method="POST" action="/admin/delete/<%= encodeURIComponent(archivePath) %>" style="margin-top:10px;">
          <button type="submit" class="btn" onclick="return confirm('Удалить логотип?')">🗑 Удалить логотип</button>
        </form>
      <% } else { %>
        <p>❌ Логотип не установлен</p>
      <% } %>
    </div>

    <div class="upload-form">
      <h3>📤 Загрузить новый логотип</h3>
      <p>Поддерживаемые форматы: SVG, PNG, JPG</p>
      <form method="POST" action="/admin/upload/<%= encodeURIComponent(archivePath) %>" enctype="multipart/form-data">
        <input type="file" name="logo" accept=".svg,.png,.jpg,.jpeg" required>
        <br><br>
        <button type="submit" class="btn">💾 Загрузить</button>
      </form>
    </div>

    <div style="margin-top: 30px;">
      <a href="/admin" class="btn">← Назад к списку</a>
      <a href="/" class="btn">🏠 На главную</a>
    </div>
  </div>
</body>
</html>