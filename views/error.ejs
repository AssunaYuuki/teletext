<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>❌ Ошибка — Телетекст Плеер</title>
  <link rel="stylesheet" href="/global.css">
  <style>
    .error-container {
      text-align: center;
      padding: 40px 20px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .error-art {
      font-size: 1.2rem;
      line-height: 1.2;
      margin-bottom: 20px;
      color: #ff6666;
      white-space: pre-line;
    }

    .error-message {
      font-size: 1.1rem;
      margin: 15px 0;
    }

    .error-actions {
      margin-top: 20px;
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      justify-content: center;
    }

    .btn {
      display: inline-block;
      padding: 10px 24px;
      background: var(--card-bg);
      border: 1px solid var(--accent);
      border-radius: 4px;
      color: var(--accent);
      text-decoration: none;
      font-weight: bold;
      transition: all 0.2s ease;
    }

    .btn:hover {
      background: var(--accent);
      color: var(--bg);
    }
  </style>
</head>
<body>

  <main class="error-container">
    <pre class="error-art">
    ❌ 404 / ОШИБКА
    =================================
    > SYSTEM FAILURE DETECTED
    > PATH NOT FOUND IN ARCHIVE
    > RETURNING TO SAFE MODE...
    =================================
  </pre>

    <p class="error-message"><%= message || 'Произошла ошибка' %></p>

    <div class="error-actions">
      <a href="/" class="btn">🏠 На главную</a>
      <button onclick="history.back()" class="btn">← Назад</button>
    </div>
  </main>



</body>
</html>